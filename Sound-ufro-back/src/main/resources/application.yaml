# |Configuración DB|
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sound_db
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true

# |Configuración Cors|